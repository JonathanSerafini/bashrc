
PATH=$(path.prepend "/usr/local/opt/coreutils/libexec/gnubin" "$PATH")
PATH=$(path.prepend "$(bashrc.home bin)" "$PATH")
PATH=$(path.prepend "$(bashrc.local bin)" "$PATH")
PATH=$(path.prepend "${HOME}/.rvm/bin" "$PATH")

export PATH

